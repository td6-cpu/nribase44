import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { motion } from "framer-motion";
import { Shield, Network, Zap, Bot, Lock, TrendingUp } from "lucide-react";

export default function StrategicMoats() {
  const moats = [
    {
      icon: Network,
      title: "Cross-Vertical Customer Journey Integration",
      description: "Property purchases naturally connect to mortgage, insurance, tax, and legal services—creating lifetime value multiplication no single-vertical competitor can replicate",
      color: "blue"
    },
    {
      icon: TrendingUp,
      title: "Network Effects & Multi-Domain Synergy",
      description: "24 domains create 276 potential integration points, amortizing customer acquisition costs across multiple revenue opportunities through 'land and expand' strategies",
      color: "green"
    },
    {
      icon: Zap,
      title: "Connector-Only Model Flexibility",
      description: "Pure directory approach enables regulatory simplicity and rapid scaling, with optionality for selective transaction facilitation based on vertical-specific performance data",
      color: "purple"
    },
    {
      icon: Bot,
      title: "AI-Powered Scalability",
      description: "Development velocity through AI-assisted coding, GEO optimization for next-generation search, and content production scale enabling volume impossible for competitors",
      color: "amber"
    },
    {
      icon: Lock,
      title: "Unified Identity & Dashboard Architecture",
      description: "Future-state single sign-on across 24 domains creating sticky user experiences and cross-vertical data network effects",
      color: "red"
    },
    {
      icon: Shield,
      title: "SEO & Content Flywheel",
      description: "24-domain backlink network provides immediate competitive advantage, with content cross-pollination and domain authority distribution creating multi-year barriers to entry",
      color: "indigo"
    }
  ];

  const colorClasses = {
    blue: { gradient: "from-blue-500 to-cyan-500", bg: "bg-blue-50", border: "border-blue-200" },
    green: { gradient: "from-green-500 to-emerald-500", bg: "bg-green-50", border: "border-green-200" },
    purple: { gradient: "from-purple-500 to-fuchsia-500", bg: "bg-purple-50", border: "border-purple-200" },
    amber: { gradient: "from-amber-500 to-orange-500", bg: "bg-amber-50", border: "border-amber-200" },
    red: { gradient: "from-red-500 to-rose-500", bg: "bg-red-50", border: "border-red-200" },
    indigo: { gradient: "from-indigo-500 to-blue-500", bg: "bg-indigo-50", border: "border-indigo-200" }
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.6 }}
      className="space-y-8"
    >
      <div>
        <h2 className="text-4xl font-bold text-slate-900 mb-4">Strategic Moats</h2>
        <p className="text-lg text-slate-600 leading-relaxed">
          Six core competitive advantages that create sustainable barriers to entry and compound over time.
        </p>
      </div>

      <div className="grid md:grid-cols-2 gap-6">
        {moats.map((moat, index) => {
          const Icon = moat.icon;
          const colors = colorClasses[moat.color];
          
          return (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              whileHover={{ y: -5, transition: { duration: 0.2 } }}
            >
              <Card className={`h-full border-2 ${colors.border} hover:shadow-2xl transition-all duration-300`}>
                <CardHeader className={colors.bg}>
                  <div className="flex items-start gap-4">
                    <div className={`w-14 h-14 rounded-xl bg-gradient-to-br ${colors.gradient} flex items-center justify-center flex-shrink-0`}>
                      <Icon className="w-7 h-7 text-white" />
                    </div>
                    <div>
                      <div className="text-sm font-semibold text-slate-500 mb-1">
                        Moat #{index + 1}
                      </div>
                      <CardTitle className="text-lg leading-tight">
                        {moat.title}
                      </CardTitle>
                    </div>
                  </div>
                </CardHeader>
                <CardContent className="pt-6">
                  <p className="text-slate-600 leading-relaxed">
                    {moat.description}
                  </p>
                </CardContent>
              </Card>
            </motion.div>
          );
        })}
      </div>

      <Card className="border-2 border-slate-300 shadow-2xl navy-card">
        <CardContent className="p-8 text-white">
          <h3 className="text-2xl font-bold mb-4">Competitive Advantage Summary</h3>
          <div className="space-y-3 text-slate-200">
            <p className="leading-relaxed">
              These six strategic moats work synergistically to create a defensible competitive position that compounds over time. The combination of network effects, cross-vertical integration, and AI-powered scalability creates barriers that become increasingly difficult for competitors to overcome.
            </p>
            <p className="leading-relaxed">
              As the platform grows, each moat reinforces the others: more domains strengthen network effects, which increases SEO authority, which enables AI-driven content scaling, which attracts more providers and customers across verticals.
            </p>
          </div>
        </CardContent>
      </Card>
    </motion.div>
  );
}
